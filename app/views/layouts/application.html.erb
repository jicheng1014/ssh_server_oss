<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Server Monitor" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Server Monitor">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <nav class="bg-white shadow">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center space-x-6">
            <%= link_to "Server Monitor", root_path, class: "text-xl font-bold text-gray-800" %>
            <div class="hidden md:flex space-x-4">
              <%= link_to "服务器列表", servers_path, class: "text-gray-600 hover:text-gray-900 #{'font-medium text-blue-600' if controller_name == 'servers' && action_name == 'index'}" %>
              <%= link_to "资源汇总", dashboard_path, class: "text-gray-600 hover:text-gray-900 #{'font-medium text-blue-600' if controller_name == 'dashboard'}" %>
            </div>
          </div>
          <div class="flex items-center">
            <%= link_to "添加服务器", new_server_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
      <% if notice.present? %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
          <%= notice %>
        </div>
      <% end %>

      <% if alert.present? %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
          <%= alert %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
