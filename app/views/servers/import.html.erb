<div class="max-w-2xl mx-auto">
  <div class="bg-white rounded-lg shadow p-6">
    <h1 class="text-2xl font-bold mb-6">导入服务器配置</h1>

    <% if flash[:alert] %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
        <%= flash[:alert].html_safe %>
      </div>
    <% end %>

    <div class="mb-6 p-4 bg-blue-50 border border-blue-200 rounded">
      <h2 class="font-semibold text-blue-900 mb-2">导入说明：</h2>
      <ul class="list-disc list-inside text-sm text-blue-800 space-y-1">
        <li>请选择之前导出的 YAML 配置文件</li>
        <li>如果服务器已存在（根据 host 判断），将更新其配置</li>
        <li>如果服务器不存在，将创建新服务器</li>
        <li>只导入用户录入的字段（name, host, port, username, active, provider, position）</li>
      </ul>
    </div>

    <%= form_with url: import_process_servers_path, method: :post, multipart: true, class: "space-y-6" do |form| %>
      <div>
        <%= form.label :yaml_file, "选择 YAML 文件", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= form.file_field :yaml_file, accept: ".yml,.yaml", class: "block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-md file:border-0 file:text-sm file:font-semibold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100" %>
        <p class="mt-1 text-sm text-gray-500">支持 .yml 或 .yaml 格式</p>
      </div>

      <div class="flex items-center gap-4">
        <%= form.submit "开始导入", class: "bg-purple-500 hover:bg-purple-700 text-white font-bold py-2 px-6 rounded cursor-pointer" %>
        <%= link_to "取消", servers_path, class: "text-gray-600 hover:text-gray-800 px-4 py-2" %>
      </div>
    <% end %>

    <div class="mt-8 p-4 bg-gray-50 border border-gray-200 rounded">
      <h3 class="font-semibold text-gray-900 mb-2">YAML 文件格式示例：</h3>
      <pre class="text-xs bg-white p-3 rounded border overflow-x-auto"><code>---
- name: 服务器1
  host: 192.168.1.100
  port: 22
  username: root
  active: true
  provider: AWS
  position: 0
- name: 服务器2
  host: example.com
  port: 22
  username: admin
  active: true
  provider: Aliyun
  position: 1</code></pre>
    </div>
  </div>
</div>
